{
  "name": "hls-fmp4-test",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "./node_modules/.bin/vite",
    "build": "./node_modules/.bin/vite build",
    "preview": "./node_modules/.bin/vite preview",
    "deploy": "pnpm run build && (find dist -type f -size +25M -delete 2>/dev/null || true) && wrangler pages deploy"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "wrangler": "^4.67.0"
  },
  "dependencies": {
    "hls.js": "^1.5.0"
  }
}
